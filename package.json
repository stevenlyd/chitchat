{
  "name": "anhao-monorepo",
  "private": true,
  "scripts": {
    "start-all": "concurrently \"cd packages/anhao-next && NODE_ENV=production PORT=$PORT bun run start\" \"cd packages/anhao-elysia && NODE_ENV=production bun run start\""
  },
  "devDependencies": {
    "concurrently": "8.2.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
